[package]
name = "odin_palace_py"
version = "0.2.0"
edition = "2024"
authors = ["Tochka"]
description = "Python bindings for odin_palace 1C bank statement parser"
license = "MIT"
repository = "https://github.com/tochka-public/odin_palace_py"
homepage = "https://github.com/tochka-public/odin_palace_py"

[lib]
name = "odin_palace_py"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.29", features = ["extension-module", "generate-import-lib"] }
odin_palace = "0.2.0"

[profile.release]
strip = true
lto = "fat"
codegen-units = 1

[lints.rust]
warnings = "deny"

[lints.clippy]
style = "deny"
perf = "deny"
all = "deny"
pedantic = "deny"
unwrap_used = "deny"
expect_used = "deny"
indexing_slicing = "deny"
fallible_impl_from = "deny"
wildcard_enum_match_arm = "deny"
fn_params_excessive_bools = "deny"
must_use_candidate = "deny"

# Локальная разработка против исходников ядра; при публикации на crates.io
# зависимость odin_palace в [dependencies] остаётся версионной.
[patch.crates-io]
odin_palace = { path = "../odin_palace" }
